<ShopFloorUI:ShopFloorCustomWindow x:Class="보령.무균공정작업자입력" 
           xmlns:ShopFloorUI="clr-namespace:ShopFloorUI;assembly=ShopFloorUI.Common"
           xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" 
           xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" 
           xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
           xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
           xmlns:sdk="http://schemas.microsoft.com/winfx/2006/xaml/presentation/sdk"
           xmlns:c1="http://schemas.componentone.com/winfx/2006/xaml"
           xmlns:i="http://schemas.microsoft.com/expression/2010/interactivity"
           xmlns:ezmes="clr-namespace:LGCNS.EZMES.ControlsLib;assembly=LGCNS.EZMES.ControlsLib"
           xmlns:local="clr-namespace:보령"
           xmlns:localctrl="clr-namespace:보령.UserControls"
           Width="400" Height="Auto"
           Title="무균공정작업자입력" x:Name="Main">
    <ShopFloorUI:ShopFloorCustomWindow.Resources>
        <local:무균공정작업자입력ViewModel x:Name="ViewModel"/>
        <Style x:Key="dgScroll" TargetType="ScrollBar">
            <Setter Property="MinWidth" Value="30"/>
            <Setter Property="MinHeight" Value="30"/>
        </Style>
    </ShopFloorUI:ShopFloorCustomWindow.Resources>
    <Grid x:Name="LayoutRoot" Background="White" DataContext="{Binding Source={StaticResource ViewModel}}">
        <i:Interaction.Triggers>
            <i:EventTrigger EventName="Loaded">
                <i:InvokeCommandAction Command="{Binding LoadedCommand}" CommandParameter="{Binding ElementName=Main}"/>
            </i:EventTrigger>
        </i:Interaction.Triggers>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="1" x:Name="MainSection">
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <Grid Grid.Row="0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="0.5*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <sdk:Label Grid.Column="0" Content="작업자ID" VerticalAlignment="Center" HorizontalAlignment="Center"/>
                <TextBox Grid.Column="1" Name="txtUserId" Margin="5" Text="{Binding VesselId, Mode=TwoWay}" KeyDown="txtUserId_KeyDown"/>
                <Button Grid.Column="2" Content="입력" Width="80" Height="30" Margin="5" HorizontalAlignment="Left" Command="{Binding CheckUserIdCommandAsync}">
                    <i:Interaction.Triggers>
                        <i:EventTrigger EventName="Click">
                            <i:InvokeCommandAction Command="{Binding StorageOutCommandAsync}"/>
                        </i:EventTrigger>
                    </i:Interaction.Triggers>
                </Button>
            </Grid>
            <Grid Grid.Row="1">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="0.5*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
            </Grid>

            <Grid x:Name="UserList" Grid.Row="2" Height="192" VerticalAlignment="Top">
                <ezmes:CNSDataGrid HeadersVisibility="Column" Height="150" ItemsSource="{Binding UserContain}" 
                                   IsRowAdd="False" IsRowDelete="True" IsEditMode="True" IsEditSection="True" IsRowNumber="False" ScrollViewer.VerticalScrollBarVisibility="Visible">
                    <ezmes:CNSDataGrid.DataColumns>
                        <ezmes:DataColumn HeadName="작업자ID" HeaderWidth="193" DataField = "USERID" ColumnType ="TextBox"/>
                        <ezmes:DataColumn HeadName="작업자명" HeaderWidth="193" DataField = "USERNAME" ColumnType ="TextBox"/>
                    </ezmes:CNSDataGrid.DataColumns>
                </ezmes:CNSDataGrid>
            </Grid>
        </Grid>


        <Grid Grid.Row="2" x:Name="Footer">
            <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center">
                <Button Margin="5,0" Name="btnComfirm" Width="100" Height="30" Content="{Binding W[CUIDataWriteToEBRButtonText], Source={StaticResource ViewModel}, FallbackValue=기록}" Command="{Binding ComfirmCommandAsync}"/>
                <Button x:Name="NoRecord" Content="기록없음" Width="100" Height="30" Command="{Binding NoRecordConfirmCommand}"/>
                <Button Margin="5,0" Name="btnClose" Width="100" Height="30" Content="{Binding W[CUICloseButtonText], Source={StaticResource ViewModel}, FallbackValue=닫기}" Click="btnCancel_Click" />
            </StackPanel>
        </Grid>
    </Grid>
</ShopFloorUI:ShopFloorCustomWindow>